# ==========================================
# Dependencies for Semi-Calibrated T-Linkage
# ==========================================

numpy>=1.22.0
opencv-python>=4.5.0
scipy>=1.7.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
numba>=0.55.0
tqdm>=4.60.0

# Dependencies for building PoseLib from source
pybind11>=2.10.0
pybind11-stubgen>=2.0.0

# ==========================================
# INSTRUCTIONS FOR POSELIB
# ==========================================
# PoseLib is required for focal length estimation.
# It must be compiled from source. Please run the following 
# commands in your terminal AFTER installing the requirements above:
#
# git clone https://github.com/vlarsson/PoseLib.git
# cd PoseLib
# pip install --no-build-isolation .
# ==========================================